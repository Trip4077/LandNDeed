import React from 'react';

const Nav = () => {
    return (
        <nav>
            <a href="#header">Home</a>
            <a href="#contact">Contact</a> 
            <a href="#about">About</a>
        </nav>
    )
}

export default Nav;